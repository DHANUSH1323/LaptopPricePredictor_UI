import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    progress: {
        alignContent: 'center'
    },
    button: {
        marginTop: "20px",
        marginBottom: "20px",
        backgroundColor: '#2C2C54',
        color: 'white'
    }
}))
